import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'mtg-score',
  webDir: 'dist/mtg-score/browser'
};

export default config;
